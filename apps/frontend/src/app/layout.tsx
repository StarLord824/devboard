import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Devboard — Draw. Think. Collaborate.",
  description:
    "A collaborative infinite canvas for developers and teams. Sketch, plan, and ship ideas together in real time.",
  keywords: ["whiteboard", "canvas", "collaboration", "devboard", "real-time"],
  openGraph: {
    title: "Devboard — Draw. Think. Collaborate.",
    description: "A collaborative infinite canvas for developers and teams.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
